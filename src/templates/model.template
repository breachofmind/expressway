module.exports = function(Model, app) {

    var <%=name%> = Model.create("<%=name%>", {
        created_at:     { type: Date, default: Date.now },
        modified_at:    { type: Date, default: Date.now }

    }).methods({
        // Model methods.
    });

    <%=name%>.expose = true;

    return <%=name%>;
}