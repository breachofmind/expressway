module.exports = function(Factory, app) {

    var <%=name%> = Factory.create("<%=name%>", {
        occurred_at:    { type: Date, default: Date.now },
        created_at:     { type: Date, default: Date.now },
        modified_at:    { type: Date, default: Date.now }

    }).methods({
        // Model methods.
    });

    <%=name%>.expose = true;

    return <%=name%>;
}