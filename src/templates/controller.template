module.exports = function(Controller, app) {

    return Controller.create("<%=name%>", function(app) {

        return {
            /**
             * @controller <%=name%>.index
             */
            index: function(request,response,next)
            {
                // Codez
            }
        };

    });
}