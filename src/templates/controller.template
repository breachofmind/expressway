"use strict";

var expressway = require('expressway');

module.exports = expressway.Controller.create('<%=name%>', function(app)
{
    return {
        index: function(request,response,next)
        {
            //
        },
    }
});