"use strict";

var Expressway = require('expressway');

class <%=name%> extends Expressway.Controller
{
    /**
     * Constructor.
     * @param app Application
     */
    constructor(app)
    {
        // Controller Setup
    }

    /**
     * "<%=name%>.index" route.
     */
    index: function(request,response,next)
    {
        //
    }
}

module.exports = <%=name%>;