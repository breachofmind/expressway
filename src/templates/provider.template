"use strict";
var Provider = require('express-mvc').Provider;

class <%=name%>Provider extends Provider
{
    constructor()
    {
        super("<%=name%>");

        this.order = 0;
        this.requires('logger');
        this.inside(ENV_CLI);
    }

    register(app)
    {
        // Codez
    }
}

module.exports = new <%=name%>Provider();