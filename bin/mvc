#!/usr/local/bin/node
/**
 * This is an example of a CLI utility for express-mvc.
 * @author Mike Adamczyk <mike@bom.us>
 */
var ExpressMVC = require('../index');

var app = ExpressMVC.init(__dirname+"/../app/", GLOBAL.ENV_CLI);
var cli = app.CLI;

/**
 * Your actions are set up in an object.
 */
cli.actions({
    mongo: function() {
        app.logger.info('Starting MongoDB Server...');
        cli.exec('mongod');
    },

    seed: function() {
        app.logger.info('Seeding...');
        require('../app/db/seeder');
    },

    test: function() {
        app.logger.info('Testing...');
        cli.exec('mocha', ['test']);
    }
});


cli.run();